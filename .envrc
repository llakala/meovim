#!/usr/bin/env bash
# the shebang is ignored, but nice for editors

watch_file default.nix
watch_file inputs.nix

watch_file packages.nix
watch_file plugins.nix

eval "$(lorri direnv --shell-file shell.nix)"
