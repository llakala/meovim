#!/usr/bin/env bash
# the shebang is ignored, but nice for editors

watch_file flake.nix

watch_file other/binaries.nix
watch_file other/optPlugins.nix
watch_file other/startPlugins.nix

watch_file other/**/*.nix

use nix
